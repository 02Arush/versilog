import { StyleSheet, Text, View } from "react-native";
import React from "react";

const log_hours = () => {
  return (
    <View>
      <Text>log_hours</Text>
    </View>
  );
};

export default log_hours;

const styles = StyleSheet.create({});
